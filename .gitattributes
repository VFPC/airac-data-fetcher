# Normalise all text files to LF in the repo.
* text=auto eol=lf

# Explicitly mark binary files so Git never tries to normalise them.
*.pdf binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.zip binary
*.exe binary
*.dll binary
